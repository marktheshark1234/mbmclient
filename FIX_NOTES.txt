ModernClient + VoidSentY0 1.0.0
================================

- Preserved original classes.wasm and runtime binaries.
- Confirmed the client core contains eagler:below_y0.
- Removed the need for the incompatible marky:height bridge.
- Cache-busted index.html runtime/wasm/helper URLs.
- Matching server plugin: VoidSentY0-1.0.0.jar for Paper 1.21.11.

This build intentionally avoids binary-patching the renderer/game core because
the required below-Y0 client protocol is already present there.
